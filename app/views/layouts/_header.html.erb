<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
        <!--ロゴ-->
        <div class="col-5">
          <a class="navbar-brand" href="/"><i class="fas fa-birthday-cake"></i><span class= "header-text"> CatJournal</span></a>
        </div>
        <!--メニュー-->
        <div class="col-7">
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
            <% if admin_signed_in? %>
              <li class="nav-item">
                <%= link_to "ユーザー", admin_users_path, class: 'nav-link text-muted' %>
              </li>
              <li class="nav-item">
                <%= link_to "投稿一覧", admin_posts_path, class: 'nav-link text-muted' %>
              </li>
              <li class="nav-item">
                <%= link_to "ログアウト", destroy_admin_session_path, method: :delete, class: 'nav-link text-muted' %>
              </li>
            <% elsif user_signed_in? %>
              <li class="nav-item">
                <%= link_to "マイページ", user_path(current_user.id), class: 'nav-link text-muted' %>
              </li>
              <li class="nav-item">
                <%= link_to "投稿一覧", posts_path, class: 'nav-link text-muted' %>
              </li>
              <li class="nav-item">
                <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: 'nav-link text-muted' %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to '新規登録', new_user_registration_path, class: 'nav-link text-muted' %>
              </li>
              <li class="nav-item">
                <%= link_to 'ログイン', new_user_session_path, class: 'nav-link text-muted' %>
              </li>
            <% end %>
          </div>

        </div>
    </div>
  </nav>