<% if current_user.followings.count > 0 %>
  <table class="table">
    <tr>
      <th></th>
      <th>name</th>
      <th>フォロー数</th>
      <th>フォロワー数</th>
    </tr>
    <% users.each do |user| %>
      <td></td>
      <td><%= user.name %></td>
      <td><%= user.followings.count %></td>
      <td><%= user.followers.count %></td>
    <% end %>
  </table>
<% else %>
  <p>ユーザーはいません</p>
<% end %>